import './NavBar.css'

const NavBar = () => {
    return (
      <nav className='container'>
       <ul>
       <a href="#">HOME</a>
       <a href="#">PRODUCT</a>
       <a href="#">ABOUT</a>
       <a href="#">CONTACT</a>
        </ul>
        </nav>
    );
  }

export default NavBar;
